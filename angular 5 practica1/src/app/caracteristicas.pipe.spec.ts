import { CaracteristicasPipe } from './caracteristicas.pipe';

describe('CaracteristicasPipe', () => {
  it('create an instance', () => {
    const pipe = new CaracteristicasPipe();
    expect(pipe).toBeTruthy();
  });
});
